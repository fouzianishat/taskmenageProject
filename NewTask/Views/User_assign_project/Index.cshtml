@model IEnumerable<NewTask.Models.User_assign_project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.project.Project_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.User_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.project.Project_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.User_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserAssignid }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserAssignid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserAssignid })
        </td>
    </tr>
}

</table>
